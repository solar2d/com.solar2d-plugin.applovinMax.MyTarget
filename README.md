Sources for the plugin `plugin.applovinMax.MyTarget`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.applovinMax.MyTarget" = {
            publisherId = "com.solar2d",
        },
    },
}
```
